[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "fast_ntt_pyo3"
version = "0.1.0"
description = "PyO3 extension for NTT."
requires-python = ">=3.8"
dependencies = [
    "galois>=0.4.6",
]
